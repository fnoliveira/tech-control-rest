server:
  port: 9090
security:
  oauth2:
    resource:
      filter-order: 3
  jwt.resource-ids: rest-server
  signing-key: MaYzkSjmkzPC57L
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:h2:~/tecdb;AUTO_SERVER=TRUE
    username: root
    password: root
    platform: h2
    hikari:
      connection-test-query: SELECT 1 FROM DUAL
      minimum-idle: 5
      maximum-pool-size: 20
      idleTimeout: 30000
      poolName: tc_as
      maxLifetime: 2000000
      connectionTimeout: 30000
      
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: false
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy